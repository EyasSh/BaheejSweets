export type ItemDisplay={
    name:string,
    price:number,
    imageName:string,
}
/**
 * @field imageName 
 * * Having the image name only as a field will enable me to access the path + the image name from anywhere which is '../../assets/imageName' 
 */